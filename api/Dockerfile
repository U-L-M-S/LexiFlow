# Placeholder Dockerfile for the API service.
# Actual build steps will be implemented in subsequent commits.
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS placeholder
WORKDIR /app
CMD ["bash", "-c", "echo 'API container scaffold pending.' && sleep infinity"]
